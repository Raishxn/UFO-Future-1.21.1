# Legacy reference files

These files are preserved for archaeology only. They are not build inputs:

- `fix_pattern.ps1` was a machine-specific, one-off source rewrite.
- `java-snapshots/` contains obsolete UTF-16 and UTF-8 copies of old mod entry
  points.

New fixes must be made in the production source tree and covered by the normal
Gradle validation.
